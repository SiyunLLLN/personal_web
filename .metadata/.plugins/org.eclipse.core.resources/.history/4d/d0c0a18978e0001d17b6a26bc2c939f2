<!DOCTYPE html>
<html>
<head><title>Home Page</title></head>
<body>
	<h1>Hello !</h1>
	<p>Welcome to!! <span th:text="${date}"></span></p>
	
	<form action = "/books/title/" method = "GET" >
		 <div class="form-group col-md-6" th:classappend="${param.error}? has-error">
		       <input class="search" type="text" name="name" id="name" autofocus="autofocus" path= "name"/>
		       <button type="submit">Search</button>
		 </div>
	</form>
	
	<button id="Button1" type="button" value="button" onclick = "parent.location='mailto:husiyun2018@gmail.com'" > Send Email
	</button>
	
 	<a href = "/books/new_book"> Create New Book> </a>


	    <table class="table table-striped table-hover">
      <thead>
        <tr>
        	<th><i class="fas fa-bolt"></i>id</th>
          <th><i class="fas fa-bolt"></i>Title</th>
          <th><i class="fas fa-calendar"></i> Author</th>
        </tr>
      </thead>
      <tbody>  <!-- <a th:href="@{/events/{num}(num=${e.id})}" th:text="${e.name}"></a></td> -->
        <tr th:each="e : ${books}" >
        <a th:href = "@{/books/{id} (id = ${e.id)}" > <td th:text = "${e.id}">Book ID</td> </a>
          <td th:text = "${e.title}">Book Title</td>
          <td th:text="${e.author}">Book Author</td>
        </tr>
      </tbody>
    </table>
</body>
</html>